shiny::runApp()
runApp()
#### read in IPEDS header file from github ####
header <- read.csv("https://raw.githubusercontent.com/emmamorgan-tufts/IPEDS_longitudinal/master/data/header_compiled.csv", check.names = F, stringsAsFactors = F)
names(header)
runApp()
runApp()
runApp()
runApp()
names(header)
table(header$`Level of institution`)
table(header$`OPE Title IV eligibility indicator code_value`)
table(header$`OPE Title IV eligibility indicator code`)
table(header$ACAD_YEAR)
table(duplicated(header$UNITID))
runApp()
runApp()
# add useful packages
pkgs <- c("tidyverse", "shiny")
for(pkg in pkgs) {
if(!require(pkg, character.only = TRUE)) {
install.packages(pkg)
library(pkg, character.only = TRUE)
}
library(pkg, character.only = TRUE)
}
#### read in IPEDS header file from github ####
header <- read.csv("https://raw.githubusercontent.com/emmamorgan-tufts/IPEDS_longitudinal/master/data/header_compiled.csv", check.names = F, stringsAsFactors = F)
runApp()
